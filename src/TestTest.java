import static org.junit.Assert.assertEquals;
import org.junit.*;
import org.junit.Test;


public class TestTest {

    @Test
  public void check() {
      test checko = new test();
      String sum = checko.overfør(250);
      assertEquals(checko.ikkelort, sum);
    }

}
